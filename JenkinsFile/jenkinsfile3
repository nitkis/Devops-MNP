pipeline {
		agent any
		stages {
			stage("hello Jenkins") {
				steps {
					sh "sudo ls -l /home"
					}
				}
			stage("displaying build number of particular job") {
				steps {
					sh "sudo echo $JOB_NAME and $BUILD_NUMBER"
					}
				}
			}
}
